# AI-Powered HR System - Architecture & Understanding Document

- The project relies on multiple autonomous agents orchestrated together.
- There are multiple files handling database connections (`connect.py`, `insert.py`), orchestration (`main.py`), and domain-specific decisions (`HR_DECISION`, `MySQL_Commnad`, `Payroll_Agent`, `Performance_agent`, `task_manager`).
- The frontend (handled in previous steps) interacts with this backend architecture conceptually.
- I am prepared to begin modifications on the backend routes and agents to hook up into the frontend we just built.
